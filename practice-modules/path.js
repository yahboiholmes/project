/* const newPath = require ('path');

const ownPath = '/Documents/project/path.js'

const pathInfo = {
    fileName: newPath.basename(ownPath),
    folderName: newPath.dirname(ownPath),
    fileExtension: newPath.extname(ownPath),
    absoluteOrNot: newPath.isAbsolute(ownPath),
    detailInfo: newPath.parse(ownPath),

}

console.log(pathInfo);

console.log(newPath.join('grandparentfolder', 'parent folder', 'child.txt'));
console.log(newPath.resolve('grand parent folder', 'parent folder', 'child.txt')); */